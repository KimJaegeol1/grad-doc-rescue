# -*- coding: utf-8 -*-
"""
core — 이 도구의 전부.  여기만 새로 쓴다.

    spec.py     검수기준 엑셀을 읽는다        ★ 정본이 코드로 들어오는 유일한 문
    prompt.py   시트 지침 + 항목표 → 프롬프트
    ask.py      모형에게 시트를 통째로 맡긴다
    calc.py     '계산' 주체 항목을 코드가 센다  (날짜 · 수 · ISBN)
    report.py   추출 내역 엑셀

왜 여기만 새로 쓰나
──────────────────
검수도구의 extract.py · compare.py 2,048줄은 사업 지식 덩어리다.
그 지식이 이제 **검수기준 엑셀**로 옮겨 갔다.  코드는 엑셀을 읽어 프롬프트로
조립하고, 모형이 낸 답을 받아 검사 칸에 옮기기만 한다.

    지식이 있는 곳   docs/보조도구 검수기준.xlsx     ← 사람이 고친다
    지식이 없는 곳   core/*.py                     ← 사람이 안 고쳐도 된다
"""
