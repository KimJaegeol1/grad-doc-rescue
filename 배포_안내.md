# 배포하기 — 보조도구

## 한 번에

```
pip install -r requirements.txt pyinstaller
python build.py
```

`보조도구_배포_YYYYMMDD_열쇠포함.zip` 이 생긴다.  이걸 나눠 준다.

`build.py` 가 여섯 단계를 밟는다.

1. **빌드 전 점검** — 라이브러리·검수기준·열쇠가 다 있나.  **없으면 여기서 멈춘다**
2. PyInstaller 로 묶기
3. 검수기준 엑셀을 exe 옆으로 — 거기가 프로그램이 찾는 자리다
4. `secrets/` 를 다시 만들기 — 손으로 넣은 것은 여기서 지워진다
5. **열쇠가 새어 나갔는지 전수 검사** — 하나라도 걸리면 zip 을 안 만든다
6. 배포용 zip — **열쇠가 들어가도 만든다**

---

## 배포본 생김새

```
보조도구/
    보조도구.exe                    ← 받는 사람이 누르는 것
    보조도구 검수기준.xlsx           ← ★ 정본.  여기를 고치면 검사 규칙이 바뀐다
    엑셀_넣기_전에_읽어주세요.txt
    secrets/                       열쇠
    프로그램파일/                    읽을거리 · 라이브러리 (수백 개)
        배포_안내.md
        배포본_점검표.txt
```

최상위에는 **누를 것 하나, 고칠 것 하나, 읽을 것 하나**만 둔다.

### ★ 검수기준 엑셀만 exe 옆에 두는 까닭

검수도구는 설정 json 을 `프로그램파일/` 안에 넣었다.  보조도구는 **검수기준
엑셀만 exe 바로 옆에 둔다.**  이것은 프로그램 설정이 아니라 **사람이 열어
고치는 물건**이기 때문이다.  라이브러리 수백 개가 든 폴더에 숨겨 두면 아무도
못 찾는다.

**규칙을 바꾸려면** 그 엑셀을 연다.  판정 기준·별칭·조문을 고치고 저장하면
다음 실행부터 그대로 먹는다.  **다시 빌드할 필요가 없다.**

> 폴더 이름 `프로그램파일` 은 세 곳이 같아야 한다 —
> `paths.py` 의 `INNER` · `build.spec` 의 `contents_directory` · `build.py` 의 `INNER`.

---

## 열쇠

**기본은 열쇠를 함께 넣는다.**  받는 사람은 풀어서 바로 실행하면 된다.

```
python build.py             열쇠 함께 (기본)
python build.py --no-keys   열쇠 없이 — .env 를 따로 전달할 때
```

들어가는 것은 `secrets/` 안의 **`.env` 와 모든 `*.json`** 이다.
빌드할 때 무엇이 들어갔는지 이름을 찍어 주므로,
**쓰지 않는 옛 열쇠 파일이 섞여 있지 않은지 그 목록을 꼭 확인할 것.**

`.env` 가 없으면 빌드가 1단계에서 멈춘다 — 모르고 열쇠 없이 배포하는 사고를 막는다.

|  | 기본 | `--no-keys` |
|---|---|---|
| zip 이름 | `…_열쇠포함.zip` | `…_배포_YYYYMMDD.zip` |
| 나눠 줄 것 | **zip 하나** | zip + `.env` 따로 |
| 받는 사람 | 풀어서 바로 실행 | `.env` 를 `secrets/` 에 넣고 실행 |
| 폴더가 밖으로 나가면 | **열쇠도 함께 나감** | 열쇠는 안 나감 |

> 열쇠가 든 zip 은 공유드라이브·메일에 그대로 올리지 않는다.
> 그래서 **파일 이름에 `열쇠포함` 을 박아 둔다** — 보면 바로 알도록.

### 검수도구와 다른 점

검수도구는 0·1단계가 열쇠 없이 돌아서, 그 사람들에게는 `--no-keys` 를 줘도
됐다.  **보조도구는 다르다.**  전부 모형이 하는 일이라 열쇠가 없으면 아무것도
못 한다.  `--no-keys` 로 만들었으면 `.env` 를 반드시 함께 전달해야 한다.

### 무엇에 쓰나

| 열쇠 | 쓰는 곳 | 없으면 |
|---|---|---|
| `.env` (OpenAI) | 검사 전체 | **아무것도 못 한다** |
| 서비스계정 json (Document AI) | 그림 PDF 읽기 | 그림 PDF 만 '못 읽음' 으로 빠진다 |

스캔한 PDF 는 글자가 없는 그림이라 Document AI OCR 로만 읽힌다.

---

## 빌드하는 PC 에 있어야 하는 것

| 라이브러리 | 없으면 |
|---|---|
| `openpyxl` | **아무것도 못 함** — 빌드가 멈춘다 |
| `openai` · `python-dotenv` | **검사 못 함** — 빌드가 멈춘다 |
| `lxml` | docx·hwpx 못 읽음 |
| `pypdf` | PDF 내장 글자 못 읽음 |
| `pyhwp` | .hwp 못 읽음 |
| `google-cloud-documentai` | 그림 PDF OCR 못 씀 |
| `tkinterdnd2` | 끌어다 놓기만 빠짐 |

위 셋(`openpyxl`·`openai`·`python-dotenv`)이 없으면 **1단계에서 멈춘다.**
나머지는 `!` 로 알려 주고 진행한다 — 그 형식만 못 읽는다.

### ★ 엔진은 `hiddenimports` 에 적어야 한다

`engines/` 의 read·ocr·write 는 동적으로 불린다.  PyInstaller 의 정적 분석은
그것을 보지 못해 통째로 빠뜨린다.  **빠져도 exe 는 만들어지고, 그 기능을
눌러야 비로소 죽는다.**

```
ModuleNotFoundError: No module named 'engines.ocr'
```

그래서 `build.spec` 의 `hiddenimports` 에 적어 두었고, `build.py` 의 4-2 단계가
빌드 뒤에 실제로 들어갔는지 Analysis TOC 로 확인한다.
**엔진을 새로 추가하면 두 곳에 이름을 넣어야 한다** —
`build.spec` 의 `hiddenimports` 와 `build.py` 의 `MUST`.

---

## 반드시 지킬 것

### ★ 빌드는 배포 대상과 같은 운영체제에서

윈도우용 exe 는 **윈도우에서만** 만들어진다.  PyInstaller 는 교차 빌드를 못 한다.

### ★ 열쇠는 손으로 넣지 않는다

`dist/` 안에 손으로 복사하지 않는다.  `build.py` 가 매번 `secrets/` 를 **통째로
지우고 새로 만들므로** 손으로 넣은 것은 사라진다.  넣으려면 그냥 `python
build.py` 를 쓴다 — 그래야 무엇이 들어갔는지 화면에 남는다.

### ★ 압축은 `build.py` 가 만든 zip 을 쓴다

윈도우 기본 압축은 한글 파일명을 깨뜨릴 때가 있다.
(실제로 `코드_구조.md` 가 `肄붾뱶_援ъ“.md` 로 깨진 적이 있다)
`build.py` 는 UTF-8 이름으로 저장한다.  **「보조도구 검수기준.xlsx」 가 깨지면
프로그램이 정본을 못 찾는다.**

---

## 받는 사람이 겪을 만한 것

**백신이 막는다**
서명 없는 exe 라 흔하다.  예외 등록하거나, 사내 배포면 코드 서명을 받는 편이 낫다.

**"Windows의 PC 보호" 파란 창**
`추가 정보` → `실행` 을 누르면 된다.  이것도 서명이 없어서다.

**"검수기준 엑셀을 찾지 못했습니다"**
`보조도구 검수기준.xlsx` 가 exe 옆에 없다.  압축을 풀 때 한글 이름이 깨졌을
수 있다.  프로그램이 그 폴더의 엑셀을 훑어 **「혹시 이건가요?」** 까지 알려 준다.

**시작이 느리다**
처음 한 번은 몇 초 걸린다.  두 번째부터 빨라진다.

---

## 고칠 일이 생기면

**규칙만 바뀐 경우** (판정 기준, 별칭, 조문, 시트·열 이름)
→ 배포본 폴더의 `보조도구 검수기준.xlsx` 만 고치면 된다.  **다시 만들 필요 없다.**

**코드가 바뀐 경우**
→ `python build.py` 를 다시 돌려 zip 을 새로 나눠 준다.
