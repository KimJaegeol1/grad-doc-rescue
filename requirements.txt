# ══ 반드시 ══════════════════════════════════════════════
# 이 셋이 없으면 프로그램이 시작조차 안 된다 (preflight.py)
openpyxl                        # 검수기준·성과관리현황 둘 다 읽고 쓴다
openai                          # 시트를 통째로 맡긴다.  이 도구의 전부다
python-dotenv                   # secrets/.env 에서 열쇠를 읽는다

# ══ 증빙 읽기 ════════════════════════════════════════════
# 없으면 그 형식만 못 읽는다.  프로그램은 돈다
lxml                            # hwp · hwpx · docx XML 파싱
pyhwp                           # .hwp (표 보존)
pypdf                           # PDF 내장 텍스트
google-cloud-documentai>=2.29.0 # 그림 PDF 를 글자로 (스캔본이 있을 때만)

# ══ 화면 ════════════════════════════════════════════════
tkinterdnd2                     # 끌어다 놓기.  없어도 클릭·단추로 다 된다

# ── 넣지 않는 것 ─────────────────────────────────────────
# xlwings   검수도구 0단계(검사 칸 만들기)가 쓰던 것.  이 도구는 칸을
#           만들지 않고 채우기만 한다.  build.spec 의 excludes 에도 있다.
#
# 열쇠는 secrets/.env 에.  secrets/README.txt 참조.