# ══ 반드시 ══════════════════════════════════════════════
openpyxl                        # 엑셀 읽기·쓰기.  전 단계가 쓴다

# ══ 0단계 · 검증열 삽입 ═══════════════════════════════════
xlwings                         # 실제 Excel 조종.  Windows/macOS + Excel 필요

# ══ 1단계 · 엑셀 단독 검증 ════════════════════════════════
# openpyxl 만 쓴다.  Excel 불필요 — 서버에서도 돈다.

# ══ 2단계 · 폴더·파일명 검증 ══════════════════════════════
openai                          # 폴더를 '뜻' 으로 매칭 · 번호 정규화
python-dotenv                   # secrets/.env 에서 키 읽기

# ══ 3단계 · 파일 개봉 ════════════════════════════════════
lxml                            # hwp·hwpx·docx XML 파싱
pyhwp                           # .hwp (표 보존)
pypdf                           # PDF 내장 텍스트
google-cloud-documentai>=2.29.0 # PDF OCR 폴백 (그림 PDF 가 있을 때만)
# openai · python-dotenv 는 위와 같은 것을 쓴다 (②③)

# ══ 화면 ════════════════════════════════════════════════
tkinterdnd2                     # 끌어다 놓기.  없어도 클릭·단추로 다 됨

# 열쇠는 secrets/.env 에.  .env.example 참조.
