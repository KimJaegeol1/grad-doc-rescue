# -*- coding: utf-8 -*-
"""
engines — 검수도구에서 그대로 가져온 층

여기 있는 셋은 **사업 지식이 없다.**  시트 이름도, 검증열도, 판정 규칙도
모른다.  파일과 칸만 다룬다.  그래서 검수도구와 이 도구가 나눠 써도
한쪽 사정으로 갈라질 일이 없다.

    read.py    파일 → 글자      hwp · hwpx · docx · pdf · 그림
    ocr.py     그림 PDF → 글자   Google Document AI
    write.py   판정 → 검사 칸    검증열을 색(FCE4D6)으로 찾아 쓴다

어디서 왔나
──────────
    검수도구 stages/stage3/read.py   →  한 글자도 안 고침
    검수도구 stages/stage3/ocr.py    →  한 글자도 안 고침
    검수도구 stages/stage3/write.py  →  6줄만 더함 (아래)

write.py 에 더한 6줄
───────────────────
검증열이 **없는** 항목을 비고에만 남기게 했다.  "증빙에서 인원을 한 명도
못 뽑았다" 같은 말은 어느 검증열에도 해당하지 않지만 사람이 알아야 한다.
판정을 쓰지 않으므로 있던 값이 지워질 일도 없다.

★ 고칠 때
──────────
이 셋은 검수도구와 **같은 물건**이다.  파일 다루는 방식이 실제로 바뀌었을
때만 손댄다.  이 도구 사정으로 바꾸고 싶어지면 그건 여기 둘 물건이 아니라
`core/` 로 갈 물건이다.
"""
