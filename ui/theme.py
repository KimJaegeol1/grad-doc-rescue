# -*- coding: utf-8 -*-
"""
gui.theme — 색 · 글꼴 · 로그 기호
================================================================
화면에 쓰이는 표기는 전부 여기 하나에 모은다.
단계(stages/)는 이 파일을 모르고, 알 필요도 없다 — 단계는 글자와 수준만 넘긴다.
"""

# ── 색 ────────────────────────────────────────────────────────
ACCENT = "#FCE4D6"      # 검증열과 같은 색 — 도구 전체 표준
BORDER = "#E0A87C"
ACC_FG = "#7A4A22"

OK_BG = "#EAF4EA"       # 드롭존 정상
BAD_BG = "#FCE8E6"      # 드롭존 어긋남
IDLE_BG = "#F0F0F0"     # 비활성 탭
PANEL = "#FAFAFA"       # 로그창 바탕
LINE = "#DDD"
MUTED = "#666"
FAINT = "#999"

# ── 글꼴 ──────────────────────────────────────────────────────
FAMILY = "맑은 고딕"
FONT = (FAMILY, 9)
FONT_B = (FAMILY, 9, "bold")
FONT_L = (FAMILY, 11, "bold")
FONT_S = (FAMILY, 8)

# ── 로그 수준 ─────────────────────────────────────────────────
# 0·1·2·3단계가 쓰던 표기를 그대로 옮겨 왔다.
#   수준 → (글자색, 굵게, 머리기호)
LEVELS = {
    "head": ("#1F5C99", True,  "■ "),   # 단계 머리말
    "done": ("#1F5C99", True,  "★ "),   # 완료 요약
    "ok":   ("#1B7F3B", False, "✓ "),   # 성공
    "warn": ("#B06000", False, "⚠ "),   # 확인 필요
    "err":  ("#C62828", False, "❌ "),  # 실패
    "skip": ("#777777", False, "⏭ "),   # 캐시 사용 · 반송
    "info": ("#333333", False, ""),     # 보통
    "stop": ("#7A4A22", True,  "● "),   # 정거장 — 사람이 볼 차례
}

RULE = "─" * 66


def prefix(level: str) -> str:
    return LEVELS.get(level, LEVELS["info"])[2]
